Отчёт по отладке приложения Луконина Виктора Викторовича 

###1. Неверная единица измерения интервала планировщика

Код :

```app/services/scheduler.py
scheduler.add_job(
    job,
    trigger="interval",
    seconds=settings.parse_schedule_minutes,
    coalesce=True,
    max_instances=1,
)
```

Параметр seconds ожидает значение в секундах, но в настройках parse_schedule_minutes хранится количество минут. В результате фоновая задача выполнялась каждые N секунд вместо N минут

Исправление :

```app/services/scheduler.py
scheduler.add_job(
    job,
    trigger="interval",
    seconds=settings.parse_schedule_minutes * 60, 
    coalesce=True,
    max_instances=1,
)
```

###2. Ошибка при старте контейнера 

```
app-1  |   File "/usr/local/lib/python3.11/site-packages/pydantic_settings/main.py", line 242, in __init__
app-1  |     super().__init__(**__pydantic_self__.__class__._settings_build_values(sources, init_kwargs))
app-1  |   File "/usr/local/lib/python3.11/site-packages/pydantic/main.py", line 250, in __init__
app-1  |     validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)
app-1  |                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
app-1  | pydantic_core._pydantic_core.ValidationError: 1 validation error for Settings
app-1  | database_url
app-1  |   Extra inputs are not permitted [type=extra_forbidden, input_value='postgresql+asyncpg://pos...stgres@db:5432/postgres', input_type=str]
app-1  |     For further information visit https://errors.pydantic.dev/2.12/v/extra_forbidden
```


В .env присутствует переменная, которая не соответствует ни одному полю в Settings

Исправлнеие:

```app/core/config.py
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        case_sensitive=False,
        extra='ignore',
    )
```



###3. Неправильная версия в requiremts

fastapi==999.0.0; python_version < "3.8"


###4.DATABSE_URL вместо DATABASE_URL и postgres_typo вместо postgres

```app/core/config.py
    database_url: str = Field(
        "postgresql+asyncpg://postgres:postgres@db:5432/postgres",
        validation_alias="DATABSE_URL",
    )
```


###5.Ошибка парсинга:

```
app-1  | Traceback (most recent call last):
app-1  |   File "/app/app/main.py", line 24, in _run_parse_job
app-1  |     await parse_and_store(session)
app-1  |   File "/app/app/services/parser.py", line 43, in parse_and_store
app-1  |     "city_name": item.city.name.strip(),
app-1  |                  ^^^^^^^^^^^^^^
app-1  | AttributeError: 'NoneType' object has no attribute 'name'
```


Из схемы данных city: Optional[ExternalCity], Это явно говорит о том, что city может отсутствовать (быть None). Исправление 


```app/services/parser.py
"city_name": item.city.name.strip() if item.city else None,
```

###6. При создании дубликата через POST /api/v1/vacancies/

выдает : Code (200)
{
  "detail": "Vacancy with external_id already exists"
}


Несоответствию документации и реального ответа. (у эндпоинта create_vacancy_endpoint указан параметр response_model=VacancyRead)

Исправление:

``` app/crud/vacancy.py
@router.post("/", response_model=VacancyRead, status_code=status.HTTP_201_CREATED)
async def create_vacancy_endpoint(
    payload: VacancyCreate, session: AsyncSession = Depends(get_session)
) -> VacancyRead:
    if payload.external_id is not None:
        existing = await get_vacancy_by_external_id(session, payload.external_id)
        if existing:
            return existing 
    return await create_vacancy(session, payload)
```


###7.Утечка HTTP-соединений в парсере

Описание проблемы:
В функции parse_and_store (файл app/services/parser.py) клиент для выполнения HTTP-запросов создавался с помощью httpx.AsyncClient без использования контекстного менеджера. Это означало, что после завершения работы или при возникновении исключений соединения не закрывались явно. Такая ситуация приводит к накоплению открытых сетевых соединений (утечка ресурсов)


###8.Неправильная инициализация множества в upsert_external_vacancies

При пустом списке external_ids переменной existing_ids присваивался пустой словарь {} вместо множества



```app/crud/vacancy.py
async def upsert_external_vacancies(
    session: AsyncSession, payloads: Iterable[dict]
) -> int:
    external_ids = [payload["external_id"] for payload in payloads if payload["external_id"]]
    if external_ids:
        existing_result = await session.execute(
            select(Vacancy.external_id).where(Vacancy.external_id.in_(external_ids))
        )
        existing_ids = set(existing_result.scalars().all())
    else:
        existing_ids = set() # было {}
```